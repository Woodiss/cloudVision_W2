apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: react-frontend
  ports:
    - protocol: TCP
      port: 5173  # 3000
      targetPort: 5173  # 3000
  type: NodePort
